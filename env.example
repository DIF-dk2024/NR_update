# Copy to .env locally (do NOT commit secrets)
SECRET_KEY=change-me-very-long-random
ADMIN_PASSWORD=change-me
DATA_DIR=/var/data
UPLOADS_DIR=/var/data/uploads
# MAX_CONTENT_LENGTH=31457280  # ~30MB in bytes
